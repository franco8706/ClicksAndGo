[pytest]
# La raíz del paquete es Python/, así que los imports `from src....` resuelven
# sin instalar el proyecto ni tocar PYTHONPATH a mano.
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
